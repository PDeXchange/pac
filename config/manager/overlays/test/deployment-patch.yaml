apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
spec:
  template:
    spec:
      containers:
      - name: manager-config
        imagePullPolicy: Never
      - name: manager-service
        imagePullPolicy: Never
